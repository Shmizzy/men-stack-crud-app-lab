<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Tavern Cafe</title>
</head>
<body>
    <h1>Menu</h1>
    <div id="menu">
        
        <% menu.forEach(element => { %>
         <a href="/menu/<%= element._id %>"><div class="menu_item"><%= element.name  %> <br> $<%= element.price  %></div></a>
        <% }) %>
        <a href="/menu/new"><div class="menu_item" >Add Menu Item <br> +</div></a>
    </div>
</body>
</html>